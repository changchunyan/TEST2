/**
 * The crmChangePlatform service.
 * @version 1.0
 */
angular.module('ywsApp').factory('CrmChangePlatformService', ['$http', '$q', 'config',
    function($http, $q, config) {
		var service = {};
		
		return service;
	}
]);